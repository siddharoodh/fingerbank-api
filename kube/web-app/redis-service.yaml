apiVersion: v1
kind: service
metadata:
  name: redis
  labels:
    name: redis-service
    app : demo-web-app

  spec:
    ports:
      - port : 6379
        targetPort: 6379
    selector:
      name:redis-pod
      app:demo-web-app
